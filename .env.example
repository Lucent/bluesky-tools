# Cloudflare Tunnel token for serving this on lucent.social.
# Create a tunnel at https://one.dash.cloudflare.com/ > Networks > Tunnels,
# add a public hostname (lucent.social) routing to http://thread-browser:8000,
# then copy the token here as .env (cp .env.example .env).
TUNNEL_TOKEN=
